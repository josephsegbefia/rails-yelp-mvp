<div class="container">
  <h1>All Restaurants</h1>
  <ul>
    <% @restaurants.each do |restaurant| %>
      <li><%= restaurant.name%>| <%= link_to "Show details", restaurant_path(restaurant)%></li>
    <% end %>
  </ul>
</div>
